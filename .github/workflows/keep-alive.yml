name: Keep Render Backend Alive

on:
  schedule:
    # Every 15 minutes
    - cron: "*/15 * * * *"

  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping backend
        run: |
          curl -fsS https://bytebuzz-backend.onrender.com/ || true